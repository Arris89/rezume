


	/*Очистка всей корзины*/

 /*   $(document).on('click', '.icon-text.icon-text_hover.all', function () {

      		 var delall = 1;

        	$.post('/local/templates/clegrand/ajax/basket.php', {delall}, function (data) {
				location.reload();
        	})

    	});
*/

    /*Нажатие на плюс*/
/*

    $(document).on('click', '.kit-number__btn.kit-number__btn_plus', function () {

      		 var addid = $(this).attr('data-id');
      		 //alert(addid);
      		 var quan = $(this).prev().text();
      		 //alert(quan);

        	$.post('/local/templates/clegrand/ajax/basket.php', {addid, quan}, function (data) {
//alert(data);
        	})

    	});
*/


     /*Нажатие на минус*/

/*
         $(document).on('click', '.kit-number__btn.kit-number__btn_min', function () {

 				 var delid = $(this).attr('data-id');
      		 //alert(delid);
      		 var delquan = $(this).next().text();
      		 //alert(delquan);

        	$.post('/local/templates/clegrand/ajax/basket.php', {delid, delquan}, function (datas) {
//alert(datas);
        	})

    	});*/

         /*Удаление комплекта*/
/*
      $(document).on('click', '.compdel', function () {

 			var compID = $(this).attr('data-id');
 			//alert(compID);

        	$.post('/local/templates/clegrand/ajax/basket.php', {compID}, function (datas) {
				//location.reload();
				//alert(datas);
        	})

    	});
*/



       /*Увеличение кол-ва комплектов*/
/*
      $(document).on('click', '.compdel', function () {

 			var compID = $(this).attr('data-id');
 			alert(compID);

        	$.post('/local/templates/clegrand/ajax/basket.php', {compID}, function (datas) {
				//location.reload();
				alert(datas);
        	})

    	});*/

          /*Уменьшение кол-ва комплектов*/

  /*    $(document).on('click', '.compdel', function () {

 			var compID = $(this).attr('data-id');
 			alert(compID);

        	$.post('/local/templates/clegrand/ajax/basket.php', {compID}, function (datas) {
				//location.reload();
				alert(datas);
        	})

    	});*/